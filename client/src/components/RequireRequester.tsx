// STUB — seam only (testing-contract.md §5). Implemented in the feat: commit.
import type { ReactNode } from 'react'

export function RequireRequester(_props: { children?: ReactNode }) {
  return null
}
