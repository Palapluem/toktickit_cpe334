/**
 * Ticket Number formatting and the calendar the year comes from.
 *
 * Authority: `docs/lab-02/specification.md` BR-04, §11.6, §11.13.
 *
 * STUB — the seam only. Created in the same commit as its tests so that they
 * fail on assertions describing the missing behaviour rather than on a missing
 * import, which `testing-contract.md` §5 rejects as red-phase evidence. Each
 * function returns a neutral value; none implements anything.
 */

export const MAX_TICKET_SEQUENCE = 999_999

export function bangkokYear(_instant: Date): number {
  return 0
}

export function formatTicketNumber(_year: number, _sequence: number): string {
  return ''
}
